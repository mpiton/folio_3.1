sonar.projectKey=folio-api
sonar.projectName=Folio API Backend
sonar.projectVersion=1.0.0
sonar.sources=src
sonar.sourceEncoding=UTF-8

# Rust specific analysis (optional - clippy report for external analyzers)
# Note: SonarQube does not natively support Rust. This report is for community plugins.
# sonar.rust.clippy.reportPaths=target/clippy-report.json

# Coverage exclusions
sonar.coverage.exclusions=**/tests/**,**/benches/**,**/target/**

# General exclusions
sonar.exclusions=**/tests/**,**/benches/**,**/target/**,**/*.toml,**/Cargo.lock
